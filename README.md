# Devops Nginx Demo

## Intro

This project helps you install NGINX on linux and the show a demo page

## Steps

sudo apt-get update
sudo apt install nginx

sudo systemctl start nginx
sudo systemctl enable nginx
